<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Courses</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <br>
                <h2>Courses</h2>
                <a href="/courses/add" class="btn btn-success float-right">Add New Course</a>
                <hr />
                {{#if courses.length}}
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Course Code</th>
                                <th>Course Description</th>
                                <th></th> <!-- Empty header for the remove button column -->
                            </tr>
                        </thead>
                        <tbody>
                            {{#each courses}}
                            <tr>
                                <td><a href="/course/{{this.courseId}}">{{this.courseCode}}</a></td>
                                <td><a href="/students?course={{this.courseCode}}">{{this.courseDescription}}</a></td>
                                <td><a href="/course/delete/{{this.courseId}}" class="btn btn-danger">Remove</a></td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
                {{else}}
                <div class="col-md-12 text-center">
                    <strong>{{message}}</strong>
                </div>
                {{/if}}
            </div>
        </div>
    </div>
</body>
</html>
